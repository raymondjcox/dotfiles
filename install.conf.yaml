- link:
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.zshrc: zshrc
    ~/.config/nvim:
      create: true
      path: neovim
      ignore-missing: true
- create:
    - ~/.local/share/nvim/swaps
    - ~/.local/share/nvim/undo
    - ~/.local/share/nvim/backups
